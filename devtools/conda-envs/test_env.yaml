name: test

channels:
  - conda-forge
  - omnia
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Requirements
  - openmm >=7.3.0
  - pyyaml
  - ambertools >=18.0
  - lxml
  - networkx
  - parmed
